# Passerelle de gestion de clés Ollama — dépendances runtime + tests.
# Épinglées en minor pour un build reproductible (dev host x86 comme hôte ARM64 : pur Python).
# Versions relevées lors de l'audit de sécurité pré-open-source (docs/SECURITY_AUDIT.md, item H1)
# pour purger les CVE connues. `pip-audit -r requirements.txt` doit rester propre côté runtime.
fastapi==0.139.2
starlette==1.3.1                # épinglé explicitement (dép. directe : app importe starlette.*)
uvicorn[standard]==0.51.0
httpx==0.28.1
jinja2==3.1.6
python-multipart==0.0.32        # parsing des formulaires HTML (admin)
itsdangerous==2.2.0             # signature des cookies de session (SessionMiddleware)
markdown==3.10.2                # rendu du manuel utilisateur (docs/manual.md) dans le panel
cryptography==49.0.0            # Fernet : chiffrement au repos des jetons des serveurs distants
pyyaml==6.0.3                   # catalogues i18n (app/locales/*.yaml) chargés par app/i18n.py
# --- tests ---
pytest==9.1.1
pytest-asyncio==1.4.0
